# TUI Components Navigation

Navigation examples for `bmux_tui_components`, including scroll areas,
selectable lists, menus, and pane scroll delegation.

Run interactively:

```sh
cargo run -p bmux_tui_components_navigation
```
