# TUI Components Navigation

Navigation examples for `bmux_tui_components`, including shared logical
scrolling, selectable lists, menus, and nested pane scroll delegation. They use
component-owned hit/focus geometry and caller-owned selection/scroll state;
applications do not maintain a parallel row or pointer map.

Run interactively:

```sh
cargo run -p bmux_tui_components_navigation
```
