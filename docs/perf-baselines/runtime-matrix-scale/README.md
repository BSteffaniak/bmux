# Runtime Matrix Scale Baselines

Optional baseline JSON files for scale scenarios generated by:

- `scripts/perf-plugin-runtime-matrix.sh --scale-profile small|medium|large --artifact-dir ...`
- `scripts/perf-plugin-runtime-matrix.sh --scale-plugin-count <N> --artifact-dir ...`

Missing baseline files are skipped by CI compare output.
