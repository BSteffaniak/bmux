# Runtime Matrix Baselines

Optional baseline JSON files for `scripts/perf-plugin-runtime-matrix.sh` scenario artifacts.

File names should match scenario slugs produced by the script (for example,
`plugin-doctor-json.json`). Missing baselines are skipped in CI compare output.
