# bmux npm packages

This directory contains npm distribution packages for bmux:

- `bmux` (unscoped main package)
- `@bmux/cli` (scoped main package)
- `@bmux/<platform>` platform-specific binary packages

Release workflows inject prebuilt binaries into platform package folders before
publishing.
