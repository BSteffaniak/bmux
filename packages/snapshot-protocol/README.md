# bmux_snapshot_protocol

Neutral snapshot protocol DTOs for bmux.

This crate defines stable snapshot status/reporting wire types shared by bmux
runtime and plugin crates. It is a protocol/model crate only and does not
implement snapshot orchestration or plugin lifecycle behavior.
