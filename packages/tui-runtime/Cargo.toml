[package]
authors     = { workspace = true }
categories  = ["command-line-interface", "gui"]
description = "Bounded scheduling and presentation runtime for bmux terminal UIs"
edition     = { workspace = true }
keywords    = ["bmux", "terminal", "tui", "runtime"]
license     = { workspace = true }
name        = "bmux_tui_runtime"
readme      = "README.md"
repository  = { workspace = true }
version     = { workspace = true }

[[example]]
name              = "image_runtime"
required-features = ["image-kitty"]

[[example]]
name              = "image_sixel"
required-features = ["image-sixel"]

[[example]]
name              = "image_iterm2"
required-features = ["image-iterm2"]

[[example]]
name              = "image_protocols"
required-features = ["image-kitty", "image-sixel", "image-iterm2", "crossterm"]

[dependencies]
bmux_image   = { workspace = true, optional = true }
bmux_tui     = { workspace = true }
futures-util = { workspace = true, features = ["std"] }
tokio        = { workspace = true, features = ["macros", "rt", "sync", "time"] }

[dev-dependencies]
bmux_keyboard = { workspace = true }

[features]
# Convenience bundle for interactive demos; individual codecs remain backend-neutral.
all-protocols    = ["image-kitty", "image-sixel", "image-iterm2", "crossterm"]
crossterm        = ["bmux_tui/crossterm"]
default          = []
fail-on-warnings = ["bmux_image?/fail-on-warnings"]
image-iterm2     = ["images", "bmux_image/iterm2"]
image-kitty      = ["images", "bmux_image/kitty"]
image-sixel      = ["images", "bmux_image/sixel"]
images           = ["dep:bmux_image"]
