[package]
authors     = { workspace = true }
categories  = ["development-tools"]
description = "Terminal models for bmux terminal multiplexer"
edition     = { workspace = true }
keywords    = ["models", "multiplexer", "pane", "terminal", "window"]
license     = { workspace = true }
name        = "bmux_terminal_models"
readme      = "README.md"
repository  = { workspace = true }
version     = { workspace = true }

[dependencies]
bmux_session_models = { workspace = true }
serde               = { workspace = true, features = ["derive"], optional = true }


[features]
# Default features
default = ["serde"]

# Development features
fail-on-warnings = ["bmux_session_models/fail-on-warnings"]

# Serialization features
serde = ["bmux_session_models/serde", "dep:serde"]
