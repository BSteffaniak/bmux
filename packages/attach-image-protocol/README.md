# bmux_attach_image_protocol

Neutral attach image transport DTOs shared by bmux runtime crates.

This crate defines stable wire types for pane image updates, image protocol
identifiers, and payload compression metadata. It is a protocol/model crate only
and does not implement attach runtime behavior or plugin orchestration.
