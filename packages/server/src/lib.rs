#![cfg_attr(feature = "fail-on-warnings", deny(warnings))]
#![warn(clippy::all, clippy::pedantic, clippy::nursery, clippy::cargo)]
#![allow(clippy::multiple_crate_versions)]
#![allow(clippy::cargo_common_metadata)]

//! Server component for bmux terminal multiplexer
//!
//! This crate provides the server functionality for managing sessions,
//! handling client connections, and coordinating the terminal multiplexer.

// TODO: Implement server functionality
