[package]
authors     = { workspace = true }
categories  = ["development-tools"]
description = "Client component for bmux terminal multiplexer"
edition     = { workspace = true }
keywords    = ["bmux", "client", "multiplexer", "terminal"]
license     = { workspace = true }
name        = "bmux_client"
readme      = "README.md"
repository  = { workspace = true }
version     = { workspace = true }

[dependencies]
# Internal bmux packages
bmux_attach_image_protocol  = { workspace = true }
bmux_attach_layout_protocol = { workspace = true }
bmux_config                 = { workspace = true }
bmux_ipc                    = { workspace = true }
bmux_perf_telemetry         = { workspace = true }
bmux_plugin_sdk             = { workspace = true }
bmux_snapshot_protocol      = { workspace = true }

# External dependencies
bmux_codec = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }
tokio      = { workspace = true, features = ["time", "sync", "rt"] }
tracing    = { workspace = true }
uuid       = { workspace = true, features = ["std"] }

[dev-dependencies]
bmux_server = { workspace = true }
tempfile    = { workspace = true }


[features]
# Default features
default = ["serde"]

# Development features
fail-on-warnings = [
    "bmux_attach_image_protocol/fail-on-warnings",
    "bmux_attach_layout_protocol/fail-on-warnings",
    "bmux_codec/fail-on-warnings",
    "bmux_config/fail-on-warnings",
    "bmux_ipc/fail-on-warnings",
    "bmux_snapshot_protocol/fail-on-warnings",
]

# Serialization features
serde = []

[package.metadata.cargo-machete]
ignored = ["bmux_server"] # dev-dependency used in integration tests
