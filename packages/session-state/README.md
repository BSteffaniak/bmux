# bmux_session_state

Neutral primitive crate hosting the reader/writer traits + registry
handle for the sessions-plugin domain. Both `packages/server` (core)
and plugin implementations depend on this crate.
