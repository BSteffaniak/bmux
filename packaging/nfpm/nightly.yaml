name: bmux-nightly
arch: ${ARCH}
platform: linux
version: ${VERSION}
section: utils
priority: optional
maintainer: Braden Steffaniak <braden@bmux.dev>
description: Nightly build of the bmux terminal multiplexer
homepage: https://github.com/BSteffaniak/bmux
license: MPL-2.0
contents:
  - src: ./dist/${TARGET}/bmux
    dst: /usr/bin/bmux
    file_info:
      mode: 0755
