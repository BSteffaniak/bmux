# bmux_decoration_plugin_api

Typed public API of the BMUX decoration plugin, generated from a BPDL
schema. Depend on this crate (not the plugin implementation) when your
plugin needs to query or adjust the current pane decoration style.
