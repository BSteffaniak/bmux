//! Moved to `bmux_clients_plugin_api::follow_state`.
//!
//! This file is intentionally empty and orphaned from the crate's
//! module tree. `FollowState` now lives in the plugin-api crate so
//! `packages/server` and other consumers can name the type without
//! depending on this plugin's impl crate.
