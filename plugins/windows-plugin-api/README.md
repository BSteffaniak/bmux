# bmux_windows_plugin_api

Typed public API of the BMUX windows plugin, generated from a BPDL
schema. Depend on this crate (not the plugin implementation) when your
plugin needs to read window/pane state, call window commands, or
subscribe to window lifecycle events.
