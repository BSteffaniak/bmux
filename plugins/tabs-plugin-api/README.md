# bmux_tabs_plugin_api

Typed public API of the BMUX tabs plugin, generated from a BPDL
schema. Depend on this crate (not the plugin implementation) when your
plugin needs to read tab/pane state, call tab commands, or
subscribe to tab lifecycle events.
