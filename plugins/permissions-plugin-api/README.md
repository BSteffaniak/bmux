# bmux Permissions Plugin API

Typed BPDL-generated contract for the bundled permissions plugin.

This crate contains schemas and generated service bindings only. Runtime state and policy implementation live in `plugins/permissions-plugin`.
