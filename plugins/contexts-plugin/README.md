# bmux_contexts_plugin

Shipped contexts plugin for bmux. Owns the typed context-layer surface
(listing contexts, creating them, selecting them, closing them).
Dependents consume this plugin via \[`bmux_contexts_plugin_api`\].

This plugin is bundled into the `bmux` CLI by default via the
`bundled-plugin-contexts` feature.
