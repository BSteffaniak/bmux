# bmux_cluster_plugin_client

Client-side native attach adapter for the cluster plugin. This crate owns `cluster://` target semantics, federated attach composition, worker stream routing, and cluster-specific attach actions while using the generic `bmux_client` attach-provider contract.
