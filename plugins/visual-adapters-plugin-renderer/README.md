# bmux visual adapters renderer

Client-side, attach-local visual adapters bundled with bmux. Adapters are registered with the generic `bmux_plugin` visual adapter registry and can be referenced by themes or render extensions without adding projection semantics to core.
