# bmux_workspaces_plugin

Owns named workspace metadata, per-client active/previous workspace selection,
and workspace lifecycle commands. Tabs remain contexts; workspaces add no PTY,
runtime, or process allocation.
